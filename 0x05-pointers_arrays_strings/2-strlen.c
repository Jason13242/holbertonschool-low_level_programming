/*
 * File: 2-strlen.c
 * Auth: Jason Ruiz
 */

#include "main.h"

/**
 * _strlen - returns the length of a string
 * @*s: pointer of the string.
 * Return: the length of the string.
 */

int _strlen(char *s)
{

	int x;

	for (x = 0 ; s[x] != '\0' ; x++)
	{
	}
	return (x);
}

/*
 * File: 5-rev_string.c
 * auth: Jason Ruiz
 */

#include "main.h"

/**
 * rev_string - reverses a string.
 * @s: string.
 * Return: Void.
 */

void rev_string(char *s)
{

	int x, y, z;

	y = 0;
	x = 0;
	while (s[y] != '\0')
	{
		y++;
	}
	s[y] = z;
	s[y] = s[x];	
}

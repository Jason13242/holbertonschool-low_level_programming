/*
 * File: 0-main.c
 * Auth: Jason Ruiz
 */

#include"main.h"

/**
 * main - tests for a function
 *         with zero
 *         to analize if the int is + o -.
 *Return: 0
 */

int main(void)
{
/*
 * i - variable created to test the function
 */
	int i;
	i = 0;
	postitive_or_negative(i);

	return (0);
}
